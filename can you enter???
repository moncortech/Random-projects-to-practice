#include <cs50.h>
#include <stdio.h>

int main(void)
{
    string name = get_string("whats your name? ");
    printf("hello, %s\n", name);
    int age = get_int("how old are you? ");
    if (age >= 18)
    {
        printf("you can enter\n");
    }
    else
    {
        printf("why are you trying to get in")
    }
}

